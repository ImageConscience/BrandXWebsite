# Brand X Commerce

A simple website for lead collection.

## Setup

1. Install dependencies: `npm install`

2. Run the server: `npm start`

3. Open http://localhost:3000 in your browser.

Leads will be stored in leads.json.